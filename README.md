# DIPP Grievance Portal API Server

## Scripts

In the project directory you can run:

### `npm start`

Runs the api server in the development mode
Open [http://localhost:8080](http://localhost:8080) to test the api.